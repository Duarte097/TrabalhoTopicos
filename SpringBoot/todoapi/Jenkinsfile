pipeline {
    agent any
    environment {
        DOCKER_IMAGE = 'todoapi-spring-app'
        DOCKER_CONTAINER = 'todoapi'
    }
    stages {
        stage('Clonar Repositório') {
            steps {
                git 'https://github.com/Duarte097/TrabalhoTopicos.git'
            }
        }
        stage('Remover Imagem e Container Existentes') {
            steps {
                script {
                    try {
                        bat "docker rm -f ${DOCKER_CONTAINER}"
                    } catch (Exception e) {
                        echo "Container não existe ou erro ao remover: ${e.message}"
                    }
                    try {
                        bat "docker rmi ${DOCKER_IMAGE}"
                    } catch (Exception e) {
                        echo "Imagem não existe ou erro ao remover: ${e.message}"
                    }
                }
            }
        }
        stage('Build da Imagem Docker') {
            steps {
                script {
                    try {
                        bat "docker build -t ${DOCKER_IMAGE} ."
                    } catch (Exception e) {
                        error "Erro ao construir a imagem Docker: ${e.message}"
                    }
                }
            }
        }
        stage('Iniciar Container Docker') {
            steps {
                script {
                    try {
                        bat "docker run -d --name ${DOCKER_CONTAINER} ${DOCKER_IMAGE}"
                    } catch (Exception e) {
                        error "Erro ao iniciar o container Docker: ${e.message}"
                    }
                }
            }
        }
    }
    post {
        always {
            script {
                emailext (
                    to: 'bioshock197@gmail.com',
                    subject: "Resultado do Job: ${currentBuild.fullDisplayName}",
                    body: "Status: ${currentBuild.currentResult}\n\nLink para o build: ${env.BUILD_URL}",
                    attachLog: true
                )
            }
        }
    }
}
